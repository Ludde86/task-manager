import React from 'react';

// this functional component will renders each task from the state (tasks array)
// each task will contain the title of the task,
// -> and two buttons
const Task = () => {
	return <li className="list-item">Task</li>;
};

export default Task;
