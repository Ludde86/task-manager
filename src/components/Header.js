import React from 'react';

// this component will just render a simple header
const Header = () => {
	return (
		<div className="header">
			<h1>Att Göra</h1>
		</div>
	);
};

export default Header;
